<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rhythme.dao.RecentWordDAO">

    <select id="findRecentWordsByUserId" resultType="com.example.rhythme.dto.RecentWordDTO">
        SELECT
        w.word,
        w.meaning,
        r.learned_at AS learnedAt
        FROM word_learning_records r
        JOIN words w ON r.word_id = w.word_id
        WHERE r.user_id = #{userId}
        ORDER BY r.learned_at DESC
        LIMIT 10
    </select>

</mapper>